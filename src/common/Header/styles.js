import { StyleSheet } from "react-native";
import { SPACING } from "@theme/constants";

export const styles = StyleSheet.create({
  container: {
    marginTop: SPACING.v110,
    marginBottom: SPACING.v20,
    marginLeft: SPACING.h15,
    width: 30,
  },
  backIcon: {
    width: 15,
    height: 26,
  },
});
